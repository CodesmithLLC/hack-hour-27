/* Merge two linked lists so that their nodes alternate. Let the first node of the zipped list be
 * the first node of the first argument, if it exists.
 * Implement the linked list using only a Node class. No need for a wrapper LinkedList class
 *
 * BONUS: Do this in place
 */


// l3 = {};
// l3 = Node(l1.value);
// l3.next = Node(l2.value);
// l3.next.next = Node(l1.next.value);
// l3.next.next = Node(l2.next.value);
// l3.next.next.next = Node(l1.next.next.value);
// l3.next.next.next.next = Node(l2.next.next.value);



function Node(val) {
  this.value = val;
  this.next = null;


}

//linkedList1
a1.value = swim
a1.next = a2
a2.value = bike
a2.next = a3
a3.value = run 
a3.next = null;

b1.value = water
b1.next = b2
b2.value = bycicle
b2.next = b3
b3.value = shoes
b3.next = null




let toggle = -1

function zip(l1, l2) {

let l3.value = l1.value 

}

module.exports = {Node: Node, zip: zip};
